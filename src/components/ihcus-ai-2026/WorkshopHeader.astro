---
import logo from "../../assets/visionxairy-icon.svg?raw";
import ThemeToggler from "../ThemeToggler.astro";

const currentPath = Astro.url.pathname;

const menuItems = [
  { name: "Home", path: "/workshops/ihcus-ai-2026", exact: true },
  { name: "Call For Papers", path: "/workshops/ihcus-ai-2026/call-for-papers" },
  { name: "Committee", path: "/workshops/ihcus-ai-2026/committee" },
  { name: "Important Dates", path: "/workshops/ihcus-ai-2026/important-dates" },
  { name: "Submission", path: "/workshops/ihcus-ai-2026/submission" },
  { name: "Contact", path: "/workshops/ihcus-ai-2026/contact" },
];

const isActive = (path: string, exact = false) => {
  if (exact) {
    return currentPath === path || currentPath === path + "/";
  }
  return currentPath.startsWith(path);
};
---

<div class="navbar bg-base-100 shadow-sm gap-2">
  <div class="navbar-start min-w-0 gap-1">
    <div class="dropdown shrink-0">
      <div tabindex="0" role="button" class="btn btn-ghost 2xl:hidden">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          class="h-5 w-5"
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M4 6h16M4 12h8m-8 6h16"></path>
        </svg>
      </div>
      <ul
        tabindex="-1"
        class="menu menu-sm dropdown-content bg-base-100 rounded-box z-1 mt-3 w-52 p-2 shadow"
      >
        {menuItems.map((item) => (
          <li>
            <a href={item.path} class={isActive(item.path, item.exact) ? "active" : undefined}
              >{item.name}</a
            >
          </li>
        ))}
      </ul>
    </div>
    <div class="flex min-w-0 items-center gap-2 px-1 py-2 sm:px-2">
      <a class="block shrink-0 text-base-content w-6 h-6 [&>svg]:w-full [&>svg]:h-full [&>svg]:fill-current" href="/" aria-label="VisionXAiRy home" set:html={logo} />
      <div class="flex min-w-0 items-center gap-2 leading-tight sm:flex-col sm:items-start sm:gap-0">
        <a class="text-base font-semibold hover:underline sm:text-xl whitespace-nowrap" href="/workshops/ihcus-ai-2026">IHCUS-AI 2026</a>
        <a class="text-[0.5rem] uppercase tracking-[0.1em] text-base-content/70 hover:underline sm:text-[0.65rem] sm:tracking-[0.18em] whitespace-nowrap" href="/">
          by VisionXAiRy
        </a>
      </div>
    </div>
  </div>

  <div class="navbar-center hidden 2xl:flex">
    <ul class="menu menu-horizontal px-1">
      {menuItems.map((item) => (
        <li>
          <a href={item.path} class={isActive(item.path, item.exact) ? "active" : undefined}
            >{item.name}</a
          >
        </li>
      ))}
    </ul>
  </div>

  <div class="navbar-end shrink-0">
    <ThemeToggler />
  </div>
</div>
